{{!-- Local overrides still work for specific cases, otherwise fall back to the theme setting --}}

{{#match @custom.card_style "card-second"}}
        {{> "cards/card-second" }}
    {{else}}
        {{#match @custom.card_style "card-third"}}
            {{> "cards/card-third" }}
        {{else}}
            {{> "cards/card-first" }}
        {{/match}}
{{/match}}